# Example config listing three experiments or file paths. Use either catalog keys
# (e.g. AMIP_CNTL) or filesystem paths (wildcards allowed) for each entry.
experiments:
  - AMIP_CNTL
  - AMIP_P4K
  - AMIP_4CO2

outdir: processed_data/divergence_levels
start: 1980
end: 1993
plvls: [50, 100, 200, 250, 300, 350, 400, 500, 600, 700, 800, 850, 900, 1000]
